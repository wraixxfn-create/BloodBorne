Vespershade - Models

Reserved for original 3D models (characters, props, environment).
Suggested layout when content arrives:
  Models/Characters
  Models/Props
  Models/Environment

Import models with clean transforms (1 unit = 1 meter), authored normals,
and naming like SM_<Category>_<Name> (e.g. SM_Prop_LanternPost).
